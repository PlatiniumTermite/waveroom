{
  "name": "waveroom",
  "version": "2.1.0",
  "description": "Real-time synchronized audio with AI Spatial Audio",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pip3 install --quiet --user yt-dlp || curl -sL https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp && chmod +x /usr/local/bin/yt-dlp || true"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "node-fetch": "^2.7.0"
  }
}
